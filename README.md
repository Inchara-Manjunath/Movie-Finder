# Movie Finder — Netflix-style (Full Stack with TMDb + Zip Code)

This repo contains a backend (Express) that proxies TMDb requests and a React + Vite frontend with a Netflix-like UI.

Follow the in-repo instructions to run locally. Remember to set TMDB_API_KEY in backend/.env.
